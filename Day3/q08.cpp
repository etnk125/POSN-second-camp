#include<iostream>
#include<algorithm>
using namespace std;
main()
{
	int n,m,k,x,min=INT_MAX,max=INT_MIN,i;
	cin>>n>>m;
	int a[n+1];
	for(i=0;i<n;i++)
	{
		cin>>a[i];
	}
	sort(a,a+n);
	for(i=0;i<m;i++)
	{
		cin>>k>>x;
	//	if(k>=0)plus[k]+=x;
//		if(k<0)minus[-k]+=x;
		
	}
	for(i=0;i<n;i++)
	{
	//	a[i]
	}
}
